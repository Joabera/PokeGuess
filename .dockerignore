# Keep the image lean — exclude everything not needed to serve predictions.
.git
.venv
__pycache__
*.pyc
data/
runs/
logs/
visualizations/
transformed_images/
card_index.json

# Training / dataset tooling and legacy scripts (not used at serve time)
scripts/
legacy/
docs/
test_pokemon_model_gui.py

# Unused weights/metadata (the app only needs the sketch + legacy models)
models/best_color.pth
models/best_color_classes.txt
